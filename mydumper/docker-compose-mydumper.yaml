services:
  mydumper_service:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - S3_ACCESS_KEY_ID
      - S3_SECRET_ACCESS_KEY
      - S3_ENDPOINT_URL
      - S3_BUCKET
    volumes:
      - /PatentDataVolume/DatabaseBackups:/DatabaseBackups
      - /airflow/PatentsView-DB:/project
    command: mydumper --host=$MYSQL_HOST --user=$MYSQL_USER --password=$MYSQL_PASSWORD
